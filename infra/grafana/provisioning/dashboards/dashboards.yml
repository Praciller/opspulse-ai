apiVersion: 1

providers:
  - name: OpsPulse dashboards
    orgId: 1
    folder: OpsPulse
    type: file
    disableDeletion: true
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
